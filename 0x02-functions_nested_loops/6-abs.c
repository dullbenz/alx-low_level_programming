/**
 * _abs - Computes the absolute value of an integer
 *
 * @n: The number for which to find the absolute value
 *
 * Return: The absolute value of n
 */
int _abs(int n)
{
	return (n < 0 ? -n : n);
}
